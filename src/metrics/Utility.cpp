#include "Utility.hpp"
#include <vector>

double Utility::calculateUtilityMul(const Allocation &alloc)
{
    // TODO : Implement the actual utility calculation logic based on the preferences of the agents and the allocation. This is a placeholder implementation that simply returns 1.0 for any allocation, which should be replaced with the actual logic to calculate the utility based on the preferences and the allocation.
    return 1.0; // Placeholder implementation, replace with actual utility calculation logic
}